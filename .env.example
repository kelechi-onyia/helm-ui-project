# ==========================================
# Helm UI Configuration
# ==========================================
# Copy this file to .env and update with your values
# Environment variables override settings in backend/config.yaml

# ==========================================
# Git Integration Settings
# ==========================================

# Enable/disable Git integration
GIT_ENABLED=true

# Git repository URL (SSH or HTTPS)
# SSH example: git@github.com:username/repo.git
# HTTPS example: https://github.com/username/repo.git
GIT_REPO_URL=git@github.com:username/repo.git

# Git branch to use
GIT_BRANCH=main

# Path to values.yaml file within the repository
GIT_VALUES_PATH=values.yaml

# Local path where the repository will be cloned
GIT_LOCAL_PATH=./git-repo

# Git commit author information
GIT_AUTHOR_NAME=Helm UI Bot
GIT_AUTHOR_EMAIL=helm-ui@example.com

# ==========================================
# Git Authentication
# ==========================================

# Authentication method: "ssh" or "token"
GIT_AUTH_METHOD=ssh

# Path to SSH private key (for SSH authentication)
# The key file should have proper permissions (chmod 600)
GIT_SSH_KEY_PATH=~/.ssh/id_rsa

# Personal access token (for HTTPS authentication)
# Only needed if using token authentication
# GIT_TOKEN=ghp_your_github_personal_access_token

# ==========================================
# Git Auto-Sync Options
# ==========================================

# Automatically pull from remote on backend startup
GIT_AUTO_PULL_ON_START=true

# Automatically commit and push on configuration updates
GIT_AUTO_PUSH_ON_UPDATE=true

# Custom commit message template (optional)
# Use {timestamp} placeholder for automatic timestamp
# GIT_COMMIT_MESSAGE_TEMPLATE=Update configuration via Helm UI at {timestamp}

# ==========================================
# Frontend Configuration
# ==========================================

# Backend API URL (used by frontend)
REACT_APP_API_URL=http://localhost:8000

# ==========================================
# Docker-specific Settings
# ==========================================

# Path to SSH keys directory (for Docker volume mounting)
# SSH_KEY_PATH=~/.ssh

# ==========================================
# Notes
# ==========================================
#
# SSH Authentication Setup:
# 1. Generate an SSH key if you don't have one:
#    ssh-keygen -t ed25519 -C "your_email@example.com"
#
# 2. Add the public key to your Git provider (GitHub, GitLab, etc.)
#
# 3. Set the path to your private key in GIT_SSH_KEY_PATH
#
# 4. Ensure correct permissions: chmod 600 ~/.ssh/id_rsa
#
# Token Authentication Setup:
# 1. Generate a personal access token from your Git provider
#
# 2. For GitHub: Settings → Developer settings → Personal access tokens
#    Required scopes: repo (full control of private repositories)
#
# 3. Set GIT_AUTH_METHOD=token
#
# 4. Set your token in GIT_TOKEN (keep this secret!)
#
# Docker Usage:
# - When using Docker, SSH keys are mounted as read-only volumes
# - The SSH_KEY_PATH variable controls which directory is mounted
# - Default: ~/.ssh directory from host machine

